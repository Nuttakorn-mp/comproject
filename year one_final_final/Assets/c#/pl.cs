using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class pl : MonoBehaviour {
    public List<Transform> pla;
    public float timer;
    float starttimer;
    // Use this for initialization
    void Start () {
        starttimer = timer;

    }
	
	// Update is called once per frame
	void Update () {
		//timer -= Time.deltaTime
	}
}
